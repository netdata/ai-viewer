// Command ai-viewer-ingest is the daemon that watches configured
// session-snapshot sources, parses them via internal/adapters, and
// writes canonical rows into the SQLite store.
//
// This file is a foundation stub. Implementation lands in subsequent
// chunks of SOW-0001 (adapter scaffolding, ingest pipeline, end-to-end
// wiring).
package main

import "fmt"

func main() {
	fmt.Println("ai-viewer-ingest: stub; implementation lands in SOW-0001 Chunks 4+")
}
