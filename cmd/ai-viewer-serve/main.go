// Command ai-viewer-serve is the HTTP + SSE server that exposes the
// canonical SQLite store to the embedded React UI and to local
// debugging via curl.
//
// This file is a foundation stub. Implementation lands in subsequent
// chunks of SOW-0001 (presenter package, REST endpoints, SSE hub,
// frontend embedding).
package main

import "fmt"

func main() {
	fmt.Println("ai-viewer-serve: stub; implementation lands in SOW-0001 Chunks 4+")
}
