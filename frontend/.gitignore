node_modules
dist
coverage
*.local
.vite
playwright-report
test-results
# throwaway fixture dir created by scripts/check-coverage-thresholds.test.sh
# (must live under frontend/ so the fixture's `import 'vitest/config'` resolves);
# the test removes it on exit, this guards against an interrupted run leaking it.
.coverage-selftest.*
# throwaway fixture dir created by scripts/check-coverage-config.test.sh
# (must live under frontend/ so the harness's relative import of
# ../check-coverage-config.mjs resolves); removed on exit, guarded against leaks.
.coverage-config-selftest.*
