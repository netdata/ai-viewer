import { ComingSoon } from '../../components/ComingSoon';

// Agents analytics (ui-pages.md §/agents) — Phase 3. Placeholder route.
export function Agents() {
  return <ComingSoon title="Agents" note="Agent usage analytics — Phase 3." />;
}
