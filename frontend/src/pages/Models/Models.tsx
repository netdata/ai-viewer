import { ComingSoon } from '../../components/ComingSoon';

// Models analytics (ui-pages.md §/models) — Phase 3. Placeholder route.
export function Models() {
  return <ComingSoon title="Models" note="Model usage analytics — Phase 3." />;
}
