import { ComingSoon } from '../../components/ComingSoon';

// Cross-session topology (ui-pages.md §/topology) — Phase 3. Placeholder route.
export function Topology() {
  return (
    <ComingSoon
      title="Topology"
      note="Cross-session force-directed actor graph — Phase 3."
    />
  );
}
