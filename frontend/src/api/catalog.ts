// Catalog endpoints (GET /api/catalog/{tools,models,agents}) — Phase 3
// (ui-pages.md §Phase Mapping). Stubbed here so the api/ surface is complete;
// the typed response shapes and hooks land with the Tools/Models/Agents pages.
// Intentionally empty for Chunk 14.
export {};
