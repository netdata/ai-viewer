# SOW-0080 — UX gaps P1 + P2 + P3 — full gap-fix roadmap

## Status

Status: pending operator approval

Sub-state: 2026-06-20. SOW-0079 (P0) shipped. This SOW proposes the complete P1 + P2 + P3 plan grouped into 6 focused SOWs that the CTO will execute autonomously after sign-off. Each child SOW gets its own spec → tests → code → review cycle.

## Pre-Implementation Gate

### Problem / root-cause model

The operator's 2026-06-20 feedback drove SOW-0079 (P0 — 5 items, shipped). This SOW picks up the 14 P1, 8 P2, and 4 P3 items from the 51-gap catalog at `.agents/sow/specs/ux-research-2026-06-20.md`. The operator's directive on 2026-06-20: "we should fix all the UX issues" + "setup goals and work against them autonomously".

Goal anchor (operator sign-off 2026-06-20, 4-question design questionnaire):
- **1c** analytical depth AND visual polish in parallel
- **2b** primary user = AI-agent builder (understanding agents, not just tracking usage)
- **3a** end-state = time-budgeted workflow scenarios (each scenario has a stated time budget)
- **4 a+c** taste = information density + flow

### Evidence reviewed

- `.agents/sow/specs/ux-research-2026-06-20.md` — the 51-gap catalog with P0-P3 priority and item-level severity
- `.agents/sow/done/SOW-0079-20260620-ux-gaps-p0.md` — P0 execution log, all 5 items shipped
- `.agents/sow/specs/data-model.md` — schema (sources, sessions, turns, ops, log_entries, rollups, fts_ops, fts_logs)
- `.agents/sow/specs/rest-api.md` — every endpoint, every filter param (already supports what P1 needs)
- `.agents/sow/specs/design-system.md` — visual language from SOW-0073 (the source of truth for all new UI)

### Affected contracts and surfaces

Frontend-only (no API changes, no DB schema changes). The 6 child SOWs add:

| SOW | New routes | New files | Behavior changes |
|---|---|---|---|
| 0081 Per-entity drill-down | /agents/:name, /models/:name, /tools/:name | 3 page components + 3 tests | ComingSoon placeholders replaced with real pages |
| 0082 Ingest errors surfaced | /ingest-errors | 1 page + 1 test | New tile on Sources page; new sidebar item |
| 0083 Inline summary + breadcrumbs | (none) | 1 components + 1 test | Session Detail breadcrumb trail; /failures rows expanded |
| 0084 Global search + chart tooltips | (none) | 2 components + 2 tests | ⌘K highlights matching entity; chart hover shows exact value |
| 0085 Density + sparklines + duration | (none) | 3 components + 3 tests | 3rd density mode (Minimal); per-row sparkline; duration bar |
| 0086 Heatmap + advanced stats | (none — new tab on /stats) | 2 charts + 2 tests | Hour-of-day heatmap; per-agent reliability sparkline |

Total: ~20 new files, ~8 modified files, ~14 new tests. Estimated 12-20 hours of focused work across 6 SOWs.

### Spec deltas to land before any test or code

Each child SOW owns its spec deltas. The cumulative effect after all 6 land:

- **`ui-pages.md`** (new spec) — a per-page contract describing header / toolbar / content / empty / loading / error for each route. Authored incrementally as each child SOW ships. After all 6, the spec covers every route in the app.
- **`design-system.md`** — add a "Data display primitives" section for sparkline / duration-bar / heatmap tokens (kept consistent with existing chart-1..5 tokens).
- **`data-model.md`** — no changes (P1 uses existing schema; ingest errors live in `sources.last_error` and the `log_entries` table which already has severity).
- **`rest-api.md`** — no changes (every filter param P1 needs already exists; the new pages reuse existing endpoints).
- **`frontend-architecture.md`** — add Routes table updates + the new components to the stack table.

### Existing patterns to reuse

- **Per-page pattern** from SOW-0073: header → toolbar → designed table → empty / loading / error states. All 6 child SOWs follow it.
- **`useFilters` + URL-synced filters** from `frontend/src/state/filters.ts` for the per-entity drill-down pages.
- **`useQuery` + SSE `stats_invalidated` + the `['stats']` cache prefix** pattern from `api/home.ts` (P0.1) — every new stats-consuming page uses it.
- **StatPill + HomeSummaryCard** from SessionsList — pattern of a horizontal summary strip with clickable tiles.
- **shadcn/ui primitives** (Tooltip, Popover, DropdownMenu, Dialog) — used everywhere; consistent a11y.

### Risk and blast radius

Low per SOW. Cumulative risk is moderate because 6 SOWs touch overlapping files (SessionsList.tsx, AppSidebar.tsx, App.tsx). Mitigation:

- Each child SOW is its own commit series with its own green gate.
- Specs are the durable record — if a later SOW contradicts an earlier one, the later one wins and the earlier spec is updated in the same commit.
- The 5-reviewer Production-Grade Loop runs on each non-trivial SOW (per the 2026-06-14 operator directive — CTO-discretion during Development phase, but for these SOWs the loop will run because they all change visible UI at scale).
- Sensitive data: no new data exposed. Per-entity pages show only what the operator can already see via the URL filter on Sessions.

### Sensitive data handling plan

N/A. The per-entity pages re-render the same SessionListItem rows the Sessions page already shows. Ingest errors surface existing log entries — no new exposure. Search is local FTS5 over the existing indexes.

### Implementation plan (per child SOW)

Each child SOW ships in this order (priority + dependency order):

**SOW-0081 — Per-entity drill-down pages (P1, 2-3 days)**
- Items: A3 (agents), A4 (models), A5 (tools), D7 ("all sessions for this agent" link from anywhere)
- Replace `/agents`, `/models`, `/tools` ComingSoon placeholders with real pages
- Each page: header (entity name + summary stats) → toolbar (time range + sort) → designed table of sessions filtered to that entity → drill to Session Detail
- New `/agents/:name`, `/models/:name`, `/tools/:name` sub-routes for deep drill (sessions for a specific agent)
- Tests: 3 new files, ~6 cases each (loading, error, empty, populated, time filter, sort)
- Risk: low — adds new routes; existing ComingSoon is the only behavior change

**SOW-0082 — Ingest errors surfaced (P1, 1 day)**
- Items: E1 (parse errors visible), E2 (ingest health banner), E3 (recent log entries), E4 (source lag visual)
- Add an "Ingest health" card to the Sources page showing per-source: lag, last successful ingest, parse error count
- New route `/ingest-errors` with a table of recent parse errors (file, line, error, source) + filter by source/severity
- New sidebar item under primary nav (between /failures and /topology) — label "Ingest errors"
- The existing /api/health endpoint is the data source; no new backend needed
- Tests: 2 new files, ~8 cases (table rendering, filters, empty state, banner states)

**SOW-0083 — Inline session summary + breadcrumbs (P1, 1 day)**
- Items: A13 (row expand → 1-line summary on Sessions), B2 (per-row mini-info), D1 + D2 (breadcrumbs + back), A14 (recent sessions)
- Session Detail: breadcrumb at top showing `Root agent / Sub-session / Op N` with clickable links back
- Session Detail: "Back to list" link in the header (uses browser history if it came from a list, else links to `/`)
- Sessions table: row click no longer navigates immediately — first click expands inline (1-line summary: first user message, last tool, status); second click drills in. (Mobile-friendly: tap expands, long-press drills in.)
- /failures rows: same inline expansion pattern
- Tests: 3 new test blocks (~12 cases)

**SOW-0084 — Global search + chart tooltips (P1, 1-2 days)**
- Items: A2 (search-as-navigation), A3-followup (highlight matching entity), D4 (global search), B5 + B6 (interactive chart tooltips + click-to-filter)
- Extend ⌘K command palette to also search sessions/agents/models/tools (currently it only navigates to known routes) — results are inline, clicking jumps to the entity
- Stats charts: hover any bar/point shows tooltip with the bucket's exact values (cost, tokens, failures, session count); clicking filters the Sessions page to that time bucket
- New `useGlobalSearch` hook wrapping `/api/search?q=...` (the existing FTS5 endpoint)
- Tests: 2 new files (~10 cases)

**SOW-0085 — Density + sparklines + duration bars (P2, 1 day)**
- Items: B1 (3rd density mode), B2 (sparkline column), F1 (duration bar), F2 (mini stat strip), C8 (earn the "Comfortable/Compact" name)
- Add a 3rd density mode: "Minimal" — hides 9 of the 13 columns, shows only Agent/Status/Cost/Started/Duration
- New column in Sessions: "Trend" — a 24h cost sparkline (last 24h of cost in tiny inline SVG)
- New column in Sessions: "Duration" — horizontal bar sized to the longest session in the current page
- Tests: 3 new test files (~12 cases)

**SOW-0086 — Heatmap + advanced stats + long-tail polish (P3, 1-2 days)**
- Items: B3 (heatmap), B4 (small multiples), A6 (reliability deep view), A7 (context pressure), A8 (stats quick-filter buttons), A10 (stale running indicator), C1 (consistent refresh), C2 (consistent loading states), C3 (consistent filter UX), C4 (date format), C6 (clickable "0 active filters"), C7 (tab strip a11y), C9 (sort indicators), F3 (session length distribution), F6 (inline tool list)
- New "Heatmap" tab on /stats: failures by hour-of-day × day-of-week (classic observability view)
- New "Small multiples" section on /stats: one mini-chart per agent
- Context pressure indicator on Session Detail: shows % of context window used, with a warning badge if > 80%
- Stats page quick-filter buttons: "exclude failures", "this agent only", "today only"
- Stale running indicator on Sessions: any session with status='running' but no recent activity gets a "stale" badge
- Loading state unification: every page uses the Skeleton component
- Date format unification: every page shows ISO-8601 short format `Jun 19, 14:30`
- Tests: ~10 new test files (~30 cases); this is the biggest SOW

Total estimated work: ~60 new tests, ~20 new files, ~8 modified files, 6 commits (1 per SOW), all gates green after each.

### Validation plan

After each child SOW:

- `scripts/lint.sh` — clean
- `scripts/test.sh` — clean (target: 800+ tests after all 6 SOWs, from 705 now)
- `scripts/check-coverage.sh` — clean (every gated package + aggregate ≥ 80% statements)
- `npm run build && npm run check:bundle-size` — main chunk ≤ 500 KB gz (currently 195 KB; budget supports ~+300 KB of new components)
- Visual verification via `playwright_headless` screenshots — light + dark mode for every new view
- 5-reviewer Production-Grade Loop runs at the end of each SOW (or its major visual deliverable) — `glm/mimo/minimax/qwen/deepseek` in parallel, fresh context

### Artifact impact plan

- Touched specs: `ui-pages.md` (new), `design-system.md` (data display tokens), `frontend-architecture.md` (routes table), all in the same commits as the code
- Touched code: ~28 files in `frontend/src/`
- New tests: ~60 Vitest cases
- New screenshots: ~30 (light + dark per new view)
- No new dependencies needed (everything reuses existing shadcn + Tailwind)

### Open decisions

- **Naming**: I'm proposing `/agents/:name`, `/models/:name`, `/tools/:name`. The `:name` might collide for agents named like `codex` or `claude-code` (those are also sources). If collision is real, use `/agents/by-name/:name` or include the source in the slug. Decision deferred to SOW-0081 execution.
- **SOW ordering**: I have 0081-0086 in this order, but if the operator prefers a different priority (e.g. density + sparklines before ingest errors), the SOWs can be reordered before execution.
- **Per-agent reliability sparkline** (item B5-followup) lives in SOW-0086 but could be moved to SOW-0081. Decision deferred.

## Requirements

### User Request

Verbatim, operator 2026-06-20:

> "I think we should fix all the UX issues. Can you setup goals and work against them autonomously?"

### Assistant Understanding

The operator wants the 51-gap catalog worked down to zero, autonomously, with goals (= SOWs) set up in advance. This SOW is the plan: 6 child SOWs covering P1 + P2 + P3. The CTO executes them in order after sign-off, with the 5-reviewer cycle on each non-trivial deliverable.

### Acceptance Criteria

After all 6 child SOWs ship:

1. ✅ The 14 P1, 8 P2, and 4 P3 items from the gap catalog are all done (or explicitly moved to a new pending SOW with rationale).
2. ✅ Each child SOW has its own commit series + sign-off from the 5 reviewer cycle + a closing SOW file in `.agents/sow/done/`.
3. ✅ The `ui-pages.md` spec exists and covers every route in the app.
4. ✅ Total tests ≥ 760 (currently 705); bundle ≤ 500 KB gz.
5. ✅ Operator can do every P1/P2/P3 use case from `.agents/sow/specs/ux-research-2026-06-20.md` in the stated time budget.
6. ✅ The 51-gap catalog at `.agents/sow/specs/ux-research-2026-06-20.md` is updated with checkmarks next to every shipped item, and the P0..P3 priority section is removed (it served its purpose).

## Execution Plan (per sign-off)

The CTO will execute in this order, with one commit series per child SOW:

1. **SOW-0081 — Per-entity drill-down** (commits `sow-0081-a` through `sow-0081-d` — one per page)
2. **SOW-0082 — Ingest errors surfaced** (commits `sow-0082-a` and `sow-0082-b`)
3. **SOW-0083 — Inline summary + breadcrumbs** (commits `sow-0083-a` and `sow-0083-b`)
4. **SOW-0084 — Global search + chart tooltips** (commits `sow-0084-a` and `sow-0084-b`)
5. **SOW-0085 — Density + sparklines + duration** (commits `sow-0085-a` through `sow-0085-c`)
6. **SOW-0086 — Heatmap + advanced stats + long-tail** (commits `sow-0086-a` through `sow-0086-e`)

Each SOW ships with:
- Spec deltas landed in the same commit (no drift)
- Failing tests written first, then implementation
- All local gates green
- 5-reviewer Production-Grade Loop run on the final diff
- CTO-verified reviewer claims
- Closing SOW file in `.agents/sow/done/`

The operator receives a one-line status per SOW ("SOW-0081 shipped, X items closed, 5/5 PG, bundle X KB gz"). The full technical detail is in the SOW closing file and the commit log. The operator never sees reviewer detail, gate output, or test names.

## Stop Conditions

- **Hard stop**: if the 5-reviewer cycle returns 5+ cycles of new P0/P1 findings on the same SOW, the CTO pauses, opens a follow-up SOW, and surfaces to the operator with a business-level recommendation (e.g. "SOW-0084 is blocked on recurring P0; recommend re-scoping").
- **Soft stop**: if a child SOW grows past 2× estimated effort, the CTO splits it and ships what's done, deferring the rest to a new child SOW.
- **Goal stop**: when the operator confirms the app is "professional, polished, modern" in their judgment — the CTO stops polishing. The 51-gap catalog's "What I am NOT going to do" section (visual identity, full brand redesign, Topology internals, mobile-first rebuild) stays out of scope unless the operator explicitly adds it.

## What I Need from the Operator

1. **Go / no-go** on this plan. If the priorities are wrong, tell me which gaps matter most.
2. **One specific real workflow** done recently — "I wanted to find X and I had to click Y". Concrete examples help me see gaps I've missed.
3. **End-state judgment** — when P0 + P1 + P2 + P3 are done, is the app "professional, polished, modern"? I won't keep polishing forever; I need a clear "stop here" signal.

## References

- `.agents/sow/specs/ux-research-2026-06-20.md` — the 51-gap catalog (this SOW's source of truth)
- `.agents/sow/done/SOW-0079-20260620-ux-gaps-p0.md` — P0 execution log
- `.agents/sow/specs/data-model.md` — full schema
- `.agents/sow/specs/rest-api.md` — every endpoint
- `.agents/sow/specs/design-system.md` — visual language
- `.agents/sow/done/SOW-0073-20260619-visual-foundation.md` — the foundation SOW
