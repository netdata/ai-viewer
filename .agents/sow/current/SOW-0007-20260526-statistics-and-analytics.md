# SOW-0007 - Statistics & Analytics (Milestone 4)

## Status

Status: in-progress

Sub-state: activated 2026-06-01 on branch `sow-0007-statistics-and-analytics`. Prerequisites met — SOW-0001 + the five adapters are in `done/` (canonical schema + REST + catalog tables; v3/v2/claude-code/codex/opencode all ship cost/token data into the store), and SOW-0006 (the viz surface that consumes analytics) is merged. Operator sign-off granted (blanket Phase-2 backlog mandate + explicit "proceed", 2026-06-01). Implementing per the 13-chunk plan in the Pre-Implementation Gate; Chunk 1 (spec deltas) lands first.

## Requirements

### Purpose

Turn the canonical store into a cross-session analytics surface. The operator needs to answer questions like "how much did I spend yesterday across all agents?", "which tool is the slowest in p99?", "which model produced the most failures this week?", "show me every session that mentioned timeout error" — fast, over millions of ops, without re-scanning raw rows on every page load. This SOW delivers the materialized rollup tables, the FTS5 search index, the new REST endpoints, the Stats dashboard, and the saved-views URL-state mechanism.

### User Request

From the Phase Mapping in `.agents/sow/specs/ui-pages.md`:

> Phase 3-5 (Milestone 4): `/topology`, `/tools`, `/models`, `/agents` cross-session analytics + advanced filters + deep search.

And from `.agents/sow/specs/data-model.md`:

> "Time-bucketed materialized rollups (per-hour and per-day) per source, per model, per tool, per agent, per provider, per cwd. Refreshed incrementally by the ingester. Avoids `SUM` over millions of rows on every page load. Schema for rollup tables is defined in SOW-0007 (Statistics & Analytics)."

The data-model spec already pre-declares this SOW by name.

### Assistant Understanding

Facts:

- The canonical schema records cost, tokens, duration, status, and dimensional metadata (provider, model, tool namespace, agent name, cwd, source format) on every op. See `.agents/sow/specs/data-model.md`.
- Catalog tables (`catalog_providers`, `catalog_models`, `catalog_tools`, `catalog_agents`, `catalog_cwds`) already exist and are refreshed by the ingester after each batch commit. They cover total-since-forever totals, not time-bucketed analytics.
- Phase 1 SOW-0001 deliberately deferred rollup tables and FTS5 to "Phase 4" — i.e. this SOW. SOW-0001 uses live aggregates over `ops` for the small Phase 1 surface.
- SQLite FTS5 is the standard built-in full-text index for SQLite; `modernc.org/sqlite` (the chosen driver) compiles FTS5 in by default.
- `GET /api/stats` already exists in `.agents/sow/specs/rest-api.md` as a Phase 1 stub returning live aggregates; this SOW promotes it to use rollups behind the scenes and adds three new endpoints: `/api/stats/aggregate`, `/api/stats/top`, `/api/search`.

Inferences:

- Hourly buckets at UTC are non-negotiable for correctness — DST and local-time rollups would silently produce wrong sums across DST transitions. UI converts UTC buckets to operator-local time for display.
- Backfill correctness requires that the rollup computation is a pure function of the op rows in the window (`(ts >= bucket_start, ts < bucket_end)`). Live-mode incremental updates and a full-recompute backfill must produce identical bytes when run on the same data — this is the proof obligation in the acceptance criteria.
- The current open hour cannot be materialized while it's still being written to; the query layer computes it on-demand by `UNION ALL`-ing a live aggregate over `ops` with the materialized rollups for closed hours.
- FTS5 index size scales linearly with text content. On the operator's workstation (294K v2 sessions × dozens of logs each = potentially 10M log entries), the index could be 1-3 GB. Acceptable, but the index is opt-in per source via a config flag so it can be disabled on memory-constrained installs.
- "Saved views" without server-side persistence is the URL-state mechanism that already exists; this SOW adds a "copy share link" button and a parser for incoming shared links — no schema additions needed.

Unknowns:

- Exact log-entry volume per session per adapter — needs measurement after SOW-0001 backfill. Drives the FTS5 index size estimate; if larger than expected, the SOW may add a "FTS over op names and error messages only, not full log bodies" mode.
- Whether SQLite FTS5 with WAL mode handles concurrent writes from the ingester without contention with the rollup refresh transaction. The store's WAL setup from SOW-0001 should cover it; worst case, FTS5 updates land in a dedicated short transaction at the tail of each ingest batch.
- Real-world cardinality of dimensional columns: how many distinct (model, provider, tool, agent, cwd) tuples exist on the operator's workstation. Drives the rollup table row count and the GROUP BY query plan. Hourly × 6 dimensions could blow up if cardinality is high; a `max_rows_per_hour` safety bound is in scope.

### Acceptance Criteria

1. **Hourly + daily rollup tables created and populated.** New tables `rollup_hourly` and `rollup_daily` per the schema in this SOW. Backfilled from existing `ops` rows in a one-shot migration; refreshed incrementally on each ingest batch commit. **Verification**: schema migration applies cleanly; backfill of the operator's existing data completes in < 5 minutes; row counts non-zero; sample bucket totals match a hand-computed `SUM(...) FROM ops WHERE ts BETWEEN ...`.
2. **Rollup correctness invariant.** For any closed hour, the value of every (cost/tokens/calls/failures) cell in `rollup_hourly` equals the result of the equivalent live aggregate over the underlying `ops` rows. **Verification**: an automated test that runs the ingester in two modes — full backfill from scratch vs incremental refresh from an empty rollup — on a shared fixture, and `diff`s the two `rollup_hourly` tables byte-for-byte (after sorting). Test passes only when identical.
3. **FTS5 cross-session search.** `GET /api/search?q=<text>` returns matching ops (by tool name, model name, error message) and matching log entries (by message body), with session/turn/op linkage. The FTS5 index is built on tool names, model names, op error messages, and log entry messages. **Verification**: full-text search across 10M log entries returns in < 500 ms p95 (bench in CI on a synthetic 10M-row fixture); search hits a known fixture string and returns the correct row; relevance ranking (FTS5 BM25) is used.
4. **`GET /api/stats/aggregate` over arbitrary timeframe + dimensions.** Parameters: `from`, `to`, `bucket` (hourly|daily), `group_by` (one or more of `model|provider|tool|agent|cwd|source_format`), `metric` (`cost|tokens_in|tokens_out|calls|failures|duration_us`). Returns time-series buckets per group. **Verification**: aggregate query over 1M ops returns in < 200 ms p95 (bench in CI); response matches a hand-computed control on a fixture.
5. **`GET /api/stats/top` top-N by dimension.** Parameters: `from`, `to`, `dimension` (`model|provider|tool|agent|cwd`), `metric`, `n` (default 20, max 200). Returns the top-N entities by the chosen metric in the timeframe. **Verification**: response order matches hand-computed `ORDER BY ... LIMIT n` against `ops`.
6. **Stats dashboard page** at `/stats` (or merged into `/`'s header — final placement TBD in Chunk 1). Charts: per-day cost / tokens / sessions / failures (line); top-N models, providers, tools, agents, cwds (horizontal bar). Uses the global FilterBar for timeframe + dimensional filtering. **Verification**: page renders against fixture; visual regression test via Playwright screenshot; axe-clean.
7. **Saved views via URL.** Filter state encodes into the URL query string (already true in SOW-0001 for filters); this SOW adds a "Copy share link" button in the FilterBar and verifies that opening the copied URL on a fresh tab restores the same filtered view exactly. **Verification**: Playwright E2E test that copies URL → opens new tab → asserts filter state matches.
8. **Specs updated**: `data-model.md` (rollup schema), `rest-api.md` (three new endpoints), `ui-pages.md` (Stats dashboard + saved-views button), `ingester.md` (rollup refresh hook + FTS5 index maintenance), `quality-gates.md` (FTS5 / rollup-correctness gates if any new ones land). **Verification**: spec drift check (`scripts/spec-drift.sh`) green.

## Analysis

Sources checked (at SOW drafting):

- `.agents/sow/specs/data-model.md` — schema, catalog tables, the "rollups deferred to SOW-0007" note.
- `.agents/sow/specs/rest-api.md` — `/api/stats`, `/api/catalog/{tools,models,agents}`, pagination conventions.
- `.agents/sow/specs/ui-pages.md` — `/tools`, `/models`, `/agents` routes; phase mapping.
- `.agents/sow/specs/canonical-events.md` — dimensions available on every op (provider, model, tool, agent via session, cwd via session, source format via session).
- `.agents/sow/specs/sse-protocol.md` — `stats_invalidated` event already specified (rate-limited 1/s); the rollup refresh fires it.
- `.agents/sow/specs/ingester.md` — batch-commit lifecycle is where the rollup refresh hook lands.

Current state:

- After SOW-0001: catalog tables populated; live aggregates serving `/api/stats`; no time-bucketed rollups; no FTS5; no `/stats` route; no `/api/search`.
- The `stats_invalidated` SSE event is wired but currently fires only when catalog tables change.

Risks:

- **R1 — Rollup table cardinality explosion**. If every hour produces (model × provider × tool × agent × cwd × source_format) tuples and each dimension has hundreds of values, hourly row counts could reach millions per week. Mitigation: the rollup primary key is `(bucket_ts, source_format, dimension_key)` where `dimension_key` is the materialized GROUP BY tuple; high-cardinality dimensions (cwd in particular) are bucketed into "top N + other" only when the operator queries by that dimension. Hourly rollup retention defaults to 90 days; daily rollup is forever.
- **R2 — Backfill correctness regressions**. The proof that live-mode and backfill-mode produce identical rollups is the core invariant. Mitigation: the dedicated diff test in Acceptance #2 is a CI gate; runs on every commit touching `internal/store/rollups.go`.
- **R3 — FTS5 index size on 10M+ log entries**. Could be 1-3 GB on the operator's workstation. Mitigation: per-source config flag (`fts5_index_logs: true|false`); default true; when false, the index covers only op names + error messages (typically < 100 MB).
- **R4 — FTS5 vs WAL contention**. Long-running FTS5 rebuild during ingest could stall reads. Mitigation: rebuilds run in a short transaction at end of each ingest batch; full rebuild (rare) is offered as a maintenance command, not auto-triggered.
- **R5 — Time-zone consistency**. Operator views in local time, store works in UTC. Mitigation: every rollup bucket is `floor(ts_utc / 3600_seconds) * 3600_seconds` (no local adjustment); UI converts for display only. Documented in spec.
- **R6 — Sub-rollup for the current open hour**. The active hour cannot be materialized yet, but aggregates that include "now" need it. Mitigation: query layer computes the current-hour sub-aggregate on-demand via live `ops` query (small row count since it's one hour of data) and `UNION ALL`s with materialized closed-hour rollups.
- **R7 — Stats page bundle weight**. Charts often pull a charting library. Mitigation: D3 is already in the bundle from SOW-0006; reuse it for line + bar charts; no new chart library. Bundle stays within the 500 KB gzipped budget.
- **R8 — Saved views URL length**. Deeply filtered views may produce > 2 KB URLs. Mitigation: query-string compression (e.g. lz-string) only if URLs cross the 2 KB threshold; otherwise plain query string. URL length asserted in test.

## Pre-Implementation Gate

Status: SATISFIED (2026-06-01) — SOW-0001 in `done/`; operator sign-off granted (blanket Phase-2 backlog mandate + explicit "proceed"). No blocking open decisions (Stats page = own `/stats` route, per Open Decisions). Cleared to implement; Chunk 1 (spec deltas) lands first.

Problem / root-cause model:

- Cross-session analytics today require `SUM` over potentially millions of `ops` rows on every page load. The catalog tables cover lifetime totals only — no time-bucketed views, no top-N over a timeframe. The operator's primary use case ("what happened yesterday across everything?") is unanswerable without materialized rollups. Additionally, no cross-session text search exists; finding all sessions that hit a specific error requires manually walking sessions. This SOW addresses both gaps with rollup tables, FTS5, and three new REST endpoints feeding a Stats dashboard.

Evidence reviewed:

- All specs cited above.
- The data-model.md "Aggregation Strategy" section that pre-declares this SOW by name.
- The Phase Mapping table in ui-pages.md placing analytics in Phase 4.
- SQLite documentation on FTS5 (built into modernc.org/sqlite); BM25 ranking is default since SQLite 3.21.

Affected contracts and surfaces:

- Schema: two new tables (`rollup_hourly`, `rollup_daily`), one new FTS5 virtual table (`fts_ops`, `fts_logs`). New migration file under `internal/store/migrations/`.
- Backend: new package `internal/rollups/`; refresh hook in `internal/ingest/`; new query helpers in `internal/store/queries.go`; three new presenter handlers in `internal/presenter/`.
- REST: three new endpoints (`/api/stats/aggregate`, `/api/stats/top`, `/api/search`); `/api/stats` continues to work but uses rollups behind the scenes.
- Frontend: new `pages/Stats/` page; new "Copy share link" button in `FilterBar`; line + bar chart renderers under `viz/` (reusing D3 from SOW-0006).
- Specs: `data-model.md`, `rest-api.md`, `ui-pages.md`, `ingester.md`, possibly `quality-gates.md`.

Existing patterns to reuse:

- Catalog table refresh pattern from SOW-0001 (`internal/ingest/catalog.go`) is the template for the rollup refresh hook.
- TanStack Query cache invalidation pattern on `stats_invalidated` SSE event (already wired).
- The URL-synced filter state from SOW-0001 covers the saved-views mechanism.
- D3 line + bar chart references via `mirrored-repos`: Grafana (line charts), Prometheus UI, Jaeger UI (top-N bars), SigNoz (dashboards).

Risk and blast radius:

- New tables only; no schema changes to existing tables. Worst case: rollup tables incorrect — drop and recompute; ingest unaffected.
- FTS5 indexes can be dropped and rebuilt without affecting source data.
- Frontend changes are additive (new page, one new button).

Sensitive data handling plan:

- FTS5 indexes log message bodies. Per the AGENTS.md sensitive-data rule, fixture log messages MUST be sanitized via `scripts/sanitize-fixture.sh` (already enforced by SOW-0001) before any `testdata/` commit.
- Search results in the UI render log message excerpts; no new sensitive-data surface vs the existing Logs tab.
- The `stats_invalidated` SSE event carries only `{ts}`; no per-session data leaks.

Implementation plan (ordered chunks):

1. **Spec deltas (lands FIRST, no code)**: rollup tables schema → `data-model.md`; `/api/stats/aggregate`, `/api/stats/top`, `/api/search` contracts → `rest-api.md`; Stats dashboard layout + saved-views button → `ui-pages.md`; rollup refresh hook + FTS5 maintenance → `ingester.md`; FTS5-disabled fallback mode documented as config flag.
2. **Schema migration**: `internal/store/migrations/000N_rollups_fts5.sql` creating `rollup_hourly`, `rollup_daily`, `fts_ops`, `fts_logs`. Idempotent.
3. **`internal/rollups/` package**: pure-function rollup computation `RollupBucket(ops []Op, bucketSize Duration) []RollupRow`; unit tests with golden tables.
4. **Backfill runner**: one-shot command `ai-viewer-ingest rollups-backfill` that scans `ops` from `MIN(ts)` to last closed hour and populates rollup tables. Idempotent (re-runnable).
5. **Incremental rollup hook**: wire into `internal/ingest/` batch commit; after each commit, recompute rollups for the affected closed hours only.
6. **Rollup correctness CI test**: runs backfill-from-scratch and incremental-from-empty on the same fixture; `diff`s the resulting tables.
7. **FTS5 index population**: build initial index on backfill; update incrementally on each ingest batch; per-source `fts5_index_logs` config flag.
8. **REST handlers**: `/api/stats/aggregate`, `/api/stats/top`, `/api/search`; new bench tests targeting the 200 ms / 500 ms p95 budgets.
9. **Stats dashboard page**: line + bar charts; integrates with FilterBar; uses TanStack Query + `stats_invalidated` invalidation.
10. **"Copy share link" button**: encodes filter state as URL; clipboard copy; Playwright E2E.
11. **a11y pass**: axe-clean on `/stats`; keyboard nav reaches every chart and control.
12. **External review round**: codex + gemini + glm + qwen per `project-second-opinions` skill.
13. **Address review findings**, re-review, mark SOW completed, move to `done/`.

Validation plan:

- Per-chunk unit + integration tests.
- Bench tests in CI for the 200 ms aggregate and 500 ms search p95 budgets.
- Rollup correctness diff test (Acceptance #2) is a CI gate.
- Playwright E2E for the Stats dashboard and the saved-views button.
- axe-clean on `/stats`.
- External review converges before SOW close.

Artifact impact plan:

- `AGENTS.md`: no expected change.
- Runtime project skills: `project-go-backend` may grow an "FTS5 maintenance" section.
- Specs: `data-model.md`, `rest-api.md`, `ui-pages.md`, `ingester.md`, possibly `quality-gates.md` updated in Chunk 1.
- End-user/operator docs: `docs/runbook.md` gets "Rollups and search" section; `docs/architecture-overview.md` mentions FTS5 disk-footprint expectations.
- End-user/operator skills: none expected.
- SOW lifecycle: standard — completed + moved to `done/` in the final commit.

Open-source reference evidence:

- FTS5 implementation reference: SQLite official docs + `clickhouse/ClickHouse` (different DB, but instructive on time-bucketed rollup patterns) — to be cited via `mirrored-repos` skill during Chunk 7 if relevant.
- Time-bucketed rollup patterns: `prometheus/prometheus` (chunked aggregation), `victoriametrics/VictoriaMetrics` (downsampling) — read for inspiration, not for code reuse.
- Chart references: `grafana/grafana` line+bar implementations.

Open decisions:

- None blocking. Stats page placement (own route `/stats` vs merged into `/`'s header strip) resolved during Chunk 1 spec delta; default is own route to keep `/` focused on the sessions list.

## Implications And Decisions

(Filled if operator surfaces design decisions during review.)

## Plan

(Mirror of Implementation Plan above; expanded with commit refs as chunks land.)

## Execution Log

### 2026-06-01

- **Chunk 1 — spec deltas (spec-first; NO code).** Landed the analytics design across 5 specs:
  - `data-model.md`: `rollup_hourly`/`rollup_daily` — **ADDITIVE LONG-FORM** (one row per dimension VALUE per bucket; PK `(bucket_ts, source_format, dimension, dimension_value)`; `dimension ∈ total|model|provider|tool|agent|cwd`; additive metric columns + `session_starts`). Chosen over the `model×provider×tool×agent×cwd` CROSS-PRODUCT to defuse R1 (cardinality explosion). **Additivity invariant** stated (daily = Σ hourly; range = Σ buckets) → makes the AC#2 backfill-vs-incremental diff gate well-defined; non-additive distinct counts excluded (the additive `session_starts` is stored instead). Open hour NEVER materialized (query `UNION ALL`s live ops). FTS5 `fts_ops`/`fts_logs` (BM25; per-source `fts5_index_logs` flag). Migration `0006_rollups_and_fts.sql` + `schema_meta.version`→6 documented. R1 `__other__` tail-collapse bound + 90-day hourly retention.
  - `rest-api.md`: `/api/stats/aggregate`, `/api/stats/top`, `/api/search` — all reuse `parseSessionFilter` + HEAD/405/BAD_REQUEST parity; `q` bound as a parameterized FTS5 MATCH; `logs_indexed:false` when log-FTS off. `/api/stats` now rollup-backed for closed buckets (same shape).
  - `ui-pages.md`: `/stats` dashboard (line: cost/tokens/sessions/failures; bar: top-N model/provider/tool/agent/cwd; deep-search box; FilterBar; D3 reuse ≤500 KB; a11y; `stats_invalidated` invalidation) + FilterBar "Copy share link".
  - `ingester.md`: incremental rollup refresh of affected CLOSED hours after batch commit (mirrors `catalog.go`, same tx, crash-safe) + FTS5 maintenance + `rollups-backfill` one-shot + open-hour-live rule.
  - `quality-gates.md`: rollup-correctness diff gate (backfill vs incremental, byte-for-byte) + aggregate/search perf benches (annotation-logged, SOW-0006 convention) + FTS5 fixture-sanitization note.
  - **Design decisions (subagent-flagged, orchestrator-confirmed):** `group_by=source_format` groups by the PK column (rollups are already per-source_format) — not a `dimension` row; `/api/stats` open-window = open-DAY for daily requests; `fts_ops` content-mode (external-content vs contentless) deferred to the Chunk-2 migration.
  - **Verified:** only the 5 specs changed (no code); contracts consistent across specs; `scan-secrets` PASS.
  - **Next:** Chunk 2 — migration `0006_rollups_and_fts.sql` (the tables) + `presenter.SchemaVersion`→6 (failing migration test first).

- **Chunk 2 — schema migration `0006_rollups_and_fts.sql` + `SchemaVersion`→6 (test-first; subagent-built, orchestrator-verified).** Created EMPTY `rollup_hourly`/`rollup_daily` (13-col additive long-form, PK `(bucket_ts, source_format, dimension, dimension_value)`, rowid tables matching the `catalog_*` convention, dimension-led index `(dimension, bucket_ts)` for the top-N/aggregate scans — `EXPLAIN QUERY PLAN`-verified) + FTS5 `fts_ops`/`fts_logs`. **FTS content-mode = content-owning** (decided this chunk; the spec deferred it): `ops.id` is `TEXT PRIMARY KEY` → no stable INTEGER rowid → external-content is structurally unavailable for ops; `fts_logs` is content-owning too for a single ingester-population path + the `fts5_index_logs=false` opt-out. Bumped `presenter.SchemaVersion` 5→6 + `schema_meta.version='6'` (`CheckSchema` exact-equality → serve refuses a pre-0006 store). Tests (red→green): table/PK/index shape, version==6, idempotency, MATCH+BM25-rank+snippet()+linkage. Cross-package test updates for the version bump (version-reference only, NOT weakened): `store_test.go` now skips the 10 FTS5 shadow tables in the table-set assertion (exact-suffix match, not a broad `fts_*` prefix); the 0005 test pins its OWN bump via an apply-through-0005 helper. `data-model.md` FTS note updated to the chosen content-owning mode. Gates (orchestrator-run): gofmt 0, vet/build 0, `go test -race ./...` all pass, gosec 0 (store + whole-repo), scan-secrets + scan-ai-attribution PASS. No rows written (backfill is Chunk 4).
  - **Next:** Chunk 3 — `internal/rollups/` pure rollup-computation package (`RollupBucket(ops) []RollupRow`, the SUM-additive fold) + golden unit tests.

- **Chunk 3 — pure `internal/rollups/` package (test-first; subagent-built, orchestrator-verified).** `Rollup(ops []OpRow, starts []SessionStart, bucket Hourly|Daily, opts Options) []RollupRow` — the SUM-additive fold: each op fans out to `total/model/provider/tool/agent/cwd` rows in its `(BucketTS, source_format)` (model only for `kind=llm`, tool only for `kind=tool` with `<ns>.<name>` id, running ops [`end_ts==nil || ≤start`] contribute duration 0); `session_starts` +1 on `total/agent/cwd` of the session's start bucket only; **deterministic** output (sorted by `(bucket,source,dimension,value)`) so the Chunk-6 backfill-vs-incremental byte-diff is well-defined; R1 `__other__` tail-collapse (cap default 2000, top-N by `op_count`, `total` never collapsed). `BucketTS` floors toward −∞ (hardened for ≤0 input). Pure — no DB, no `time.Now`. Tests: golden fan-out, the **additivity property (Daily == Σ Hourly)**, session_starts, `__other__` collapse + tie-break, edges. Gates (orchestrator re-verified after a stale-LSP false alarm — `go build`/`vet`/`test -race` all clean): gofmt 0, vet 0, golangci 0, gosec 0. NO DB / endpoints (Chunks 4/8).
  - **Next:** Chunk 4 — backfill runner (`ai-viewer-ingest rollups-backfill`): read `ops`+`sessions` from the store → map to `rollups.OpRow`/`SessionStart` → `Rollup(...)` → upsert into `rollup_hourly`/`rollup_daily` (closed buckets only); idempotent/re-runnable.

- **Chunk 4 — design decisions (pre-implementation; CTO).** Recorded BEFORE tests/code per spec→test→code. **Spec tightening landed first** (precision, not behavior change): `data-model.md` §"Open-bucket rule" rewritten to pin **independent cutoffs per granularity** — `rollup_hourly` materializes every bucket `< floor(now,hour)` (**incl. the current day's already-closed hours**); `rollup_daily` materializes every bucket `< floor(now,day)` (**excl. the entire open day**), so the current day's closed hours are in `rollup_hourly` but the current day has NO `rollup_daily` row, and the daily=Σhourly identity holds only for fully-closed days. `ingester.md` §"One-shot backfill" + §"Incremental rollup refresh" updated to the same independent cutoffs. WHY: this boundary is the seam the Chunk-6 backfill-vs-incremental byte-diff compares; an unpinned boundary = a guaranteed gate failure that per-chunk review can't see. **Backfill design:** (1) heavy logic in `internal/ingest` (`BackfillRollups(ctx, db, now, logger)`), testable + reused by Chunk 5; `cmd/ai-viewer-ingest` gets only a thin `rollups-backfill` subcommand dispatch (mirrors the daemon→`ingest.New/Start` split). (2) **Single ordered `ops` pass** (`ORDER BY start_ts`, `start_ts < floor(now,hour)`) buffered per UTC day → bounded memory (one day of ops, never the whole table); session starts preloaded into a per-day map (sessions are bounded, one row each). (3) Each day-window folded via the **same pure `rollups.Rollup`** the Chunk-5 per-hour recompute uses → diff-gate parity by construction; per-day flush upserts hourly always, daily only for days `< floor(now,day)`. (4) **Upsert** `ON CONFLICT(bucket_ts,source_format,dimension,dimension_value) DO UPDATE SET <metrics>=excluded.<metrics>` (full overwrite); `ops` are append-only ⇒ a recompute never needs to delete a vanished key ⇒ no `DELETE` ⇒ idempotent/re-runnable. (5) **`now` injectable** (UTC µs) so the closed-bucket boundary is deterministic under test. (6) **Shared op→`OpRow` / session→`SessionStart` mapping** (both chunks use it verbatim): `source_format` from `sources.format` (== canonical, written by `ensureSourceRow`); `Failed = status=='failed'`; `EndTS` from `ops.end_ts` (NULL ⇒ running); `DurationUS` from `ops.duration_us` (0 if NULL). (7) FTS population is **Chunk 7** — this chunk builds rollups only. (8) **Multi-source parity note for Chunk 5:** rollup rows are keyed per `source_format`, and the Chunk-5 incremental hook runs inside a single-source writer (knows `w.sourceFormat`), so it must recompute only the affected buckets' rows **for its own `source_format`** (scope the per-window recompute by the writer's `source_id`/`source_format`); the backfill instead derives `source_format` per row via the `ops→sessions→sources` join and computes every source in one pass. Both produce the same `(bucket, source_format, …)` rows ⇒ diff-gate parity.
  - **Next:** Chunk 4 implementation — delegate to a backend subagent (test-first); orchestrator verifies gates + commits spec+tests+code together.

- **Chunk 4 — `rollups-backfill` runner (test-first; subagent-built, orchestrator-verified).** `ingest.BackfillRollups(ctx, db, now, logger) (BackfillStats, error)` in NEW `internal/ingest/rollup_backfill.go`: computes `openHourStart`/`openDayStart` from the injected `now`, preloads all closed session starts into a per-UTC-day map, discovers the sorted union of (closed-op days ∪ session-start-only days), and per day opens ONE tx → reads that day's ops → folds via `rollups.Rollup(...)` for Hourly (always) + Daily (only when `day < openDayStart`) → idempotent `ON CONFLICT … DO UPDATE SET <metrics>=excluded` upsert. Thin `rollups-backfill` subcommand: 6-line dispatch in `cmd/ai-viewer-ingest/main.go` `run()` (daemon path untouched, `--version`/`--help` preserved) → `runBackfill` (`cmd/ai-viewer-ingest/backfill.go`) reusing `resolveDBPath`+`newLogger`, opening `store.OpenWriter`, passing `time.Now().UTC().UnixMicro()`. **Design change forced by ground truth (subagent diagnosed, orchestrator-verified):** the brief's single global ordered-ops cursor DEADLOCKS — `store.OpenWriter` pins `SetMaxOpenConns(1)` (`store.go:146`), so a `*sql.DB` read cursor held across a per-day `BeginTx` blocks forever. Restructured to discover the closed-day list first (drained), then read each day's ops **inside that day's own tx** (`tx.QueryContext`, drained before upserts) — mirrors the established `writer.go` "all reads via `tx.*`" pattern; **semantics + per-day memory bound + the shared `scanOpRow` mapping (Chunk-5 reuse) are unchanged**. Cutoffs exactly per the tightened spec (hourly incl. current-day closed hours, daily excl. open day; verified by `…_OpenHourExclusion` + `…_OpenDayVsClosedHour` tests). Orchestrator folded two `gopls` modernizations as trivial-verified format fixes (`sort.Slice`→`slices.Sort` on `[]int64`; explicit `min(...)`). Stale-LSP false alarm noted+dismissed (`nullIfEmpty redeclared`/`runBackfill unused` — `go build` clean; only one `func nullIfEmpty` in `writer.go`). Tests (11; red→green): T1 basic correctness (hand-computed SUM + every dimension + session_starts-not-on-model/provider/tool), T2 open-hour exclusion, T3 open-day-vs-closed-hour, T4 idempotency (2 runs identical), T5 multi-source split, T6 failed-status, T7 running-op (0 duration), T8 session-start-only day, T9 empty-DB, T10 cmd dispatch (on-disk DB) + bad-DB exit. Gates (orchestrator-run from repo root): gofmt 0, `go build ./...` 0, `go vet` 0, **golangci-lint 0** (`/usr/bin/golangci-lint` — the subagent only checked `~/go/bin` and skipped it; orchestrator ran the real CI gate), `go test -race` PASS (ingest + cmd), gosec 0, scan-secrets + scan-ai-attribution PASS; live smoke (empty DB) exits 0 with the no-op log. FTS untouched (Chunk 7).
  - **Next:** Chunk 5 — incremental rollup refresh hook in the ingester (mirror `catalog.go`): after each batch commit, recompute the affected CLOSED-hour buckets (exclude open hour) + affected days (exclude open day) for the batch's `source_format`, reusing `scanOpRow`; same `rollups.Rollup` recompute+overwrite → diff-gate parity with Chunk 4.

- **Chunk 5 — design decisions (pre-implementation; CTO).** Map (from a read-only investigation): the batch flow is `worker.flush()` → per-event `apply()` loop (`worker.go:171`) → `refreshAggregates` (`:176`) → `emitNotify` (`:190`) → `tx.Commit` (`:197`); catalog hooks run per-event inside the loop. **Hook seam:** a new `wr.refreshRollups(ctx, tx)` injected between the apply loop (`:174`) and `refreshAggregates` (`:176`) — batch-level, pre-commit, in the SAME tx (crash-safe, mirrors catalog). **Affected-bucket tracking:** add `dirtyRollupBuckets map[int64]struct{}` (UTC-hour starts) on the writer, cleared in `resetBatch`; mark `BucketTS(ts,Hourly)` on exactly the three rollup-affecting events — `OpStarted` (op start), `OpFinalized` (op's PERSISTED start_ts — finalize changes duration/status so its bucket MUST refresh), `SessionStarted` (session_starts). TurnStarted/SessionUpdated/PayloadRef/LogEntry do NOT touch rollups (logs→FTS is Chunk 7). **Recompute per affected `(bucket, w.sourceFormat)`:** DELETE the `(bucket, source_format)` rows then INSERT the fresh fold — **delete-then-insert, NOT overwrite** — because as cardinality grows a value can fall into `__other__`, and overwrite would strand the pre-collapse row; delete-then-insert keeps the cell authoritative. Hourly for every dirty hour `< openHourStart`; daily for every derived day `BucketTS(H,Daily) < openDayStart` (reads the whole day's ops for the source). Scoped to `w.sourceID`/`w.sourceFormat` so other sources' rows are untouched (multi-source parity). **Diff-gate float-determinism (critical):** both the Chunk-4 day query AND the Chunk-5 hour/day queries MUST `ORDER BY o.start_ts ASC, o.id ASC` — without the `id` tiebreak, equal-`start_ts` ops fold in physical-rowid order, which differs between the backfill DB and the replayed-incremental DB → ULP-divergent `cost_usd` sums → byte-diff FAIL. Chunk 5 refactors the shared SELECT/JOIN into a const reused by both paths and **adds the `o.id` tiebreak to Chunk 4's `dayOpsQuery`** (parity fix). **Injectable `now`:** add `ingest.WithNow(func() int64)` (default `time.Now().UTC().UnixMicro()`) threaded to the writer so the Chunk-6 diff gate drives backfill and incremental with the SAME cutoff. **Notify:** extend the `emitNotify` `stats_invalidated` condition to `len(affectedSessionIDs)>0 || len(dirtyRollupBuckets)>0` (still ≤1/batch; today's session-write condition is already a superset, so this is spec-literal + future-proof). **Assumption (matches catalog + SOW-0027):** an op never moves buckets post-start (adapters emit authoritative start at/before finalize); cross-bucket op migration is out of scope.
  - **Next:** Chunk 5 implementation — delegate (test-first); orchestrator verifies gates + commits.

- **Chunk 5 — incremental rollup refresh hook (test-first; subagent-built, orchestrator-verified).** NEW `internal/ingest/rollup_refresh.go`: `(*writer).refreshRollups(ctx, tx)` called in `worker.flush()` between the apply loop and `refreshAggregates` (in the SAME batch tx → atomic with the ops it summarizes). Per-batch `dirtyRollupBuckets map[int64]struct{}` (UTC-hour starts), cleared in `resetBatch`, marked at exactly 3 sites: `applySessionStarted`(ev.Ts), `applyOpStarted`(ev.Ts), `applyOpFinalized`(**persisted `ops.start_ts`**, guarded `startTs.Valid` — NOT `ev.Ts`/the end; finalize changes duration/status which roll up into the op's START hour). For each dirty hour `< openHourStart` and each derived day `< openDayStart`: **DELETE the `(bucket, w.sourceFormat)` rows then INSERT the fresh `rollups.Rollup` fold** (delete-then-insert, NOT overwrite — a value that fell into `__other__` must not strand its old per-value row), source-scoped via `s.source_id` so sibling sources' rows in the same bucket are untouched. **Diff-gate float-determinism:** refactored the shared SELECT/JOIN into `rollupOpSelectFrom` + `sessionStartSelectFrom` (shared `scanOpRow`/`scanSessionStart`) and **added `, o.id ASC` to Chunk-4's `dayOpsQuery`** + the new window queries — equal-`start_ts` ops now fold in a deterministic total order on both paths (else ULP-divergent `cost_usd` sums fail the byte-diff). Injectable clock: `ingest.WithNow(func() int64)` (default `time.Now().UTC().UnixMicro()`) threaded ingester→worker→writer so Chunk 6 drives both paths at the same cutoff. `emitNotify` `stats_invalidated` now fires on `affectedSessionIDs ∪ dirtyRollupBuckets` (still ≤1/batch). Assumption documented (matches catalog + SOW-0027): an op never moves buckets post-start. **Modernize hints NOT applied** (CTO call, evidence-based): the `gopls` rangeint/maps.Copy/WaitGroup.Go hints are not enforced by the repo's golangci-lint and those idioms are ABSENT from the existing code (the pre-existing `mergeExtras`/`ingester` hits prove it) — adopting them in one file would break "blend into existing patterns". Tests (9; red→green) in `rollup_refresh_test.go`: **parity-with-backfill** (incremental vs `BackfillRollups` byte-identical incl. equal-`start_ts` tiebreak + 2100-cwd `__other__` collapse), open-bucket exclusion, replay idempotency, `__other__` stale-row removal, multi-source isolation, session_starts attribution, single notify, OpFinalized-refreshes-start-bucket (×2 incl. finalize-end-in-later-hour). Gates (orchestrator-run): gofmt 0, build 0, vet 0, **golangci-lint 0**, gosec 0, full `go test -race ./internal/ingest/...` PASS (147.75s — shared files changed, so the WHOLE package was re-raced, not just new tests). FTS untouched (Chunk 7).
  - **Next:** Chunk 6 — backfill-vs-incremental byte-diff CI gate (`internal/ingest` test): build two stores from the SAME events + SAME `now` — one via per-batch incremental refresh, one via `BackfillRollups` from scratch — and assert `rollup_hourly`+`rollup_daily` are byte-identical (sorted). This is the executable proof the two paths agree (`quality-gates.md` §Rollup correctness diff).

- **Chunk 6 — rollup-correctness diff gate (test-first; subagent-built, orchestrator-verified) + it immediately CAUGHT a P1 in Chunk 5.** NEW `internal/ingest/rollup_parity_test.go` `TestRollupParity_AllFixtures`: ingests all 38 real adapter fixtures (claude_code/codex/aiagent_v2/aiagent_v3) through the full ingester (incremental path) at a fixed far-future `now` (every fixture bucket closed → max coverage), snapshots `rollup_hourly`+`rollup_daily`, wipes them, runs `BackfillRollups(sameNow)`, snapshots again, and asserts **byte-identical** (every PK, every column, `cost_usd` EXACT — no epsilon) + non-vacuous guards (both tables non-empty, ≥1 daily, ≥1 non-`total` dim, ≥1 format with ≥2 distinct `source_id`s). **The gate earned its keep on first use:** its initial draft used a UNIQUE synthetic `source_format` per fixture and passed — but that DODGED a real bug. Reworked to NATURAL per-adapter formats (same-adapter fixtures share a `source_format` across distinct `source_id`s) → the gate went RED, exposing a **P1 in Chunk 5's incremental refresh**: `deleteBucketRows` deleted by `source_format` but `windowOpsQuery`/`windowSessionStartsQuery` re-read by `source_id`, so when two sources share a format in one bucket the recompute wiped the shared cell and re-inserted only its own ops — silently dropping the sibling's contribution and **violating AC#2** (the rollup PK is `source_format`; `BackfillRollups` aggregates all `source_id`s of a format; a documented `--source claude_code:/a --source claude_code:/b` config hits this). **Adjudicated on ground truth** (read the 4 lines), rejected the subagent's "escape-hatch only / 1:1 auto-discovery" downgrade — the data model mandates by-`source_format` aggregation, so it is a correctness bug regardless of auto-discovery. **Fix:** `rollup_refresh.go` window queries now read by `src.format = w.sourceFormat` (symmetric with the DELETE, identical to backfill); no-op for the common 1-source-per-format deployment. **Red-proof:** reverting only the 4 lines makes BOTH the new regression test AND the parity gate fail (incremental 147 vs backfill 159 hourly rows). New regression `TestRefreshRollups_AggregatesMultipleSourcesSameFormat` (two `claude_code` sources, one bucket → summed). `MultiSourceIsolation` confirmed to use two DIFFERENT formats (genuine cross-format isolation) → unchanged. Spec hardened: `ingester.md` §"Incremental rollup refresh" now mandates `source_format` scoping for BOTH the DELETE and the re-read. Post-fix gate: hourly=159, daily=127, byte-identical. Gates (orchestrator-verified): gofmt 0, build 0, vet 0, **golangci-lint 0**, gosec 0, full `go test -race ./internal/ingest/...` PASS. **The entire rollup ENGINE is now complete and proven: one-shot backfill (Chunk 4) + in-batch incremental (Chunk 5) + executable byte-equality proof (Chunk 6).** Validates the recorded lesson — a holistic diff gate catches seam defects invisible to per-chunk review.
  - **Next:** Chunk 7 — FTS5 index population (`fts_ops` + `fts_logs`).

- **Chunk 7 — design decisions (pre-implementation; CTO; from a read-only investigation).** Deliver the FULL FTS subsystem (no half-built feature): population (ops + logs) **and** the spec'd per-source `fts5_index_logs` opt-out. **Migration `0007`:** `ALTER TABLE sources ADD COLUMN fts5_index_logs INTEGER NOT NULL DEFAULT 1`; bump `SchemaVersion` 6→7 (`schema_meta.version='7'` + `presenter.SchemaVersion` + the cross-package version-reference test updates — mirror Chunk 2's 5→6 pattern exactly; `CheckSchema` exact-equality → a v7 serve refuses a pre-0007 store). WHY persisted (not an in-memory option): the `rollups-backfill` subcommand is a SEPARATE process and `rest-api.md`'s per-source `logs_indexed` must be queryable. **Config:** ingester `WithFTS5IndexLogs(sourceID, bool)` (mirror `WithSourceFormat`/`WithLocation`/`WithNow`), persisted by `ensureSourceRow` (default true). **`fts_ops`** (content-owning, `op_id` is TEXT → FTS5 has no UPSERT): **DELETE-then-INSERT by `op_id`**; indexed text = `name`, `model`, `provider`, `tool_namespace`, `error_text` (= `error_class` + `error_message`, non-empty parts joined). Hook in `applyOpStarted` (initial index, reuse the catalog `inserted`/`prior` flags) + `applyOpFinalized` (re-index once error text is persisted). **`fts_logs`** (content-owning, `log_id` is INTEGER rowid): plain INSERT after the `log_entries` INSERT (logs are append-only — no delete), **gated on the source's `fts5_index_logs`** (read once, cache on the writer). **Backfill:** the `rollups-backfill` subcommand also rebuilds FTS from scratch (`ingester.md` §"One-shot backfill") — `DELETE FROM fts_ops; DELETE FROM fts_logs;` then bulk-INSERT all ops + all logs (logs only for flag=true sources); idempotent/re-runnable. **Incremental:** in-batch, SAME tx as rollups/catalog (atomic), rides the existing `stats_invalidated` notify (no new signal — `affectedSessionIDs ∪ dirtyRollupBuckets` already covers it). **Tests:** MATCH + `bm25` rank + `snippet()` over a populated index (mirror `migration_0006_rollups_fts_test.go`); op re-index on finalize (error text appears); **`fts5_index_logs=false` ⇒ zero `fts_logs` rows** for that source while `fts_ops` still populates; **backfill-vs-incremental FTS parity** (mirror the Chunk-6 rollup diff gate — `fts_ops`/`fts_logs` byte-identical between the two paths); empty/edge. **Sequencing within the chunk:** land migration 0007 + version bump + config plumbing FIRST (verify the version ripple is green across packages), THEN the two population paths. Delegate test-first; orchestrator verifies gates + commits.
  - **Next:** Chunk 7 implementation (then Chunk 8 — REST endpoints `/api/stats/aggregate|top` + `/api/search` reading rollups + FTS).

- **Chunk 7a — FTS schema + config plumbing (test-first; subagent-built, orchestrator-verified).** Migration `0007` (`ALTER TABLE sources ADD COLUMN fts5_index_logs INTEGER NOT NULL DEFAULT 1`) + `SchemaVersion` 6→7 (`schema_meta.version='7'` + `presenter.SchemaVersion`; `CheckSchema` exact-equality → a v7 serve refuses a pre-0007 store). Ingester `WithFTS5IndexLogs(sourceID, bool)` (mirrors `WithSourceFormat`; default **true** = opt-OUT), resolved per-source and PERSISTED by `ensureSourceRow` (INSERT + `ON CONFLICT(id) DO UPDATE SET fts5_index_logs=excluded.fts5_index_logs` → a daemon restart re-asserts the configured value). Flag is persisted only — NO FTS read/gate yet (that's 7b). Threading is `Ingester→worker.fts5IndexLogs→ensureSourceRow`; deliberately kept OFF `writer` (a write-only field would trip the `unused` lint + extending `newWriter` would break ~6 unlisted test callers — the worker field is the read site). Version-ripple swept across store/presenter tests (the 0006 own-bump pin moved to an apply-through-0006 internal helper, mirroring 0005; `schema_contract_test.go` `sources` contract gains `fts5_index_logs`). Stale-LSP false alarm dismissed by ground truth (`newWriter WrongArgCount` / `worker MissingLitField` diagnostics were a mid-edit snapshot of a reverted signature experiment — `go build`/`go vet` exit 0; final `newWriter` is unchanged). Tests: migration version==7 + column default 1 + idempotent + pre-0007 rejected; option persists 0/1; restart re-assert. Gates (orchestrator-verified): gofmt 0, build 0, vet 0, **golangci 0**, gosec 0, store+presenter `-race` PASS, ingest `-race` PASS.
  - **Next:** Chunk 7b — FTS population: backfill rebuild + in-batch incremental of `fts_ops` (DELETE-then-INSERT by `op_id`; text = name/model/provider/tool_namespace/error_text) and `fts_logs` (append, gated on `fts5_index_logs`); backfill-vs-incremental FTS parity gate mirroring the rollup diff gate.

- **Chunk 7b — FTS5 index population (test-first; subagent-built, orchestrator-verified). Chunk 7 COMPLETE.** `fts_ops` (always indexed) maintained by `(*writer).refreshFTS` (NEW `internal/ingest/fts_refresh.go`) — a post-apply-loop, in-tx hook (called in `worker.flush` right after `refreshRollups`) driven by a per-batch `dirtyOpIDs` set (marked in `applyOpStarted`+`applyOpFinalized`, cleared in `resetBatch`); each dirty op is DELETE-then-INSERT by `op_id` (FTS5 has no UPSERT) from its FINAL persisted columns (a started+finalized-in-one-batch op indexes its `error_text`; orphan finalize → DELETE-only). `error_text` = `error_class`+`error_message` via the SHARED `composeErrorText` (both paths use it → cannot drift — the parity property). `fts_logs` (append-only, gated on the source's `fts5_index_logs`) inserted inline in `applyLogEntry`, keyed by the `log_entries.id` from `RETURNING id` (`ErrNoRows` on the `ON CONFLICT DO NOTHING` replay → skip, no duplicate; matches `BackfillFTS` reading the already-deduped `log_entries`). `writer.fts5IndexLogs` set per batch from `worker.fts5IndexLogs` (mirrors `wr.now`; `newWriter` signature unchanged). One-shot `BackfillFTS` (NEW `fts_backfill.go`) wired into the `rollups-backfill` subcommand (`cmd/ai-viewer-ingest/backfill.go`): `DELETE` both FTS tables then bulk-INSERT all ops + flag=true sources' logs; idempotent. **Load-bearing correction the FTS parity gate caught (adjudicated on ground truth):** the backfill's `fts_logs` query must resolve the owning source THROUGH the session (`log_entries le JOIN sessions s ON le.session_id=s.id JOIN sources src ON s.source_id=src.id WHERE le.session_id IS NOT NULL AND src.fts5_index_logs=1`) — `applyLogEntry` only ever indexes SESSION-scoped logs (`source_id` NULL; the source-scoped pricing-miss/source-error/orphan logs are NOT indexed), so a naive `log_entries.source_id` join would index the WRONG set and diverge; the parity gate forced the correct join (proven: 22 `fts_logs` rows byte-identical). Tests (8): incremental MATCH/bm25/snippet, op re-index on finalize (no dup), `fts5_index_logs` gating on/off (logs gated, ops always), log-replay no-dup, backfill (mixed flag)+idempotent+empty, and **`TestFTSParity_AllFixtures`** (incremental vs `BackfillFTS` byte-identical by LOGICAL columns over 38 fixtures/4 formats — fts_ops=84, fts_logs=22; compares indexed text + UNINDEXED keys, not internal FTS5 docids). Gates (orchestrator-verified): gofmt 0, build 0, vet 0, **golangci 0**, gosec 0, full `go test -race` (ingest+cmd) PASS. Stale-LSP `refreshFTS unused` diagnostic dismissed (wired at worker.go:210; golangci `unused` would fail otherwise).
  - **Next:** Chunk 8 — rollup+FTS-backed REST endpoints.

- **Chunk 8 — design decisions (pre-implementation; CTO).** Split: **8a** = `/api/stats/aggregate` + `/api/stats/top` (rollup-backed); **8b** = `/api/search` (FTS5). **Spec tightened first** (`rest-api.md` §aggregate): the long-form rollups are per-SINGLE-dimension, so they natively answer only `sources`(source_format)+`from`/`to` alongside the requested `group_by`/`dimension`. **Rollup fast path** (filters ⊆ {from, to} ONLY): closed buckets summed from `rollup_hourly`/`rollup_daily` (`dimension=? AND bucket_ts ∈ [from, openBucketStart)`). **Live fold** (ANY of `agents`/`models`/`tools`/`status`/`q`/**`sources`** present): the whole `[from,to)` aggregated live from `ops⋈sessions`. (`sources` forces the live fold because it binds to `sessions.source_id` — finer than the rollups' `source_format` key, so a `source_format` filter would over-count sibling sources; confirmed `filters.go:348`.) **Open bucket(s) ALWAYS live-folded.** Critically, BOTH the open-bucket fold AND the live-fold path compute via the SAME pure `internal/rollups.Rollup` (presenter reads the relevant `ops`→`rollups.OpRow` + session starts, calls `Rollup(...)`), so open/live results are byte-consistent with the materialized closed buckets BY CONSTRUCTION — the same parity property the ingester's diff gate guarantees. Presenter gets a local `scanRollupOpRow` (mirrors ingest's `scanOpRow`; small duplicate beats coupling presenter↔ingest) + a **parity test** (rollup-fast-path == live-fold for a `sources`+time query over a closed range). `group_by=source_format` → `dimension='total'` keyed by `source_format`; `group_by=total` → single `''` series; `metric=calls`→`op_count`; bucket hour/day cutoffs reuse `rollups.BucketTS`. 8b (`/api/search`): FTS5 `MATCH` (bound `?q`), `bm25` rank best-first, `snippet()`; ops + logs joined back to `ops`/`sessions` to apply `parseSessionFilter` (fts tables only carry op_id/session_id/log_id linkage); `logs_indexed` reflects whether any in-scope source has `fts5_index_logs=1`; offset-style cursor mirroring `/api/sessions/:id/logs`. Mirror presenter handler/filter/cursor patterns (`handleStats`, `parseSessionFilter`/`whereClause`, `cursor.go`, `writeJSON`/`writeDBError`, `withQueryTimeout`).
  - **Next:** Chunk 8a implementation (aggregate + top), then 8b (search).

- **Chunk 8a — `/api/stats/aggregate` + `/api/stats/top` (test-first; subagent-built, orchestrator-verified).** Two rollup-backed presenter handlers mirroring `handleStats` (GET/HEAD guard → enum parse → `parseSessionFilter` → `withQueryTimeout` → `writeJSON`/`writeDBError`). NEW `stats_rollup.go` (shared: `statsMetric` enum `sm*` + metric→rollup-column/RollupRow-field maps, `parseBucket`/`parseGroupBy`/`parseTopDimension`/`parseMetric`, `isRollupFastPath`, `loadClosedRollups`, `loadFoldOps`/`foldOpsWindow`, presenter-local `scanRollupOpRow`), `stats_aggregate.go`, `stats_top.go`; +2 routes in `presenter.go`; `writeBadEnum` in `query.go`. **All live/open folding goes through the pure `internal/rollups.Rollup`** → open bucket + live-fold are byte-consistent with materialized closed rows by construction. **CTO correction (deviated from my prompt, verified correct):** the `sources` filter binds to `sessions.source_id` (`filters.go:348`) which is FINER than the rollups' `source_format` — filtering rollups by source_format would over-count sibling sources (the multi-source case), so `isRollupFastPath` requires `sources` empty too; the fast path is **{from,to}-only**, everything else (incl. `sources`) live-folds. I corrected `rest-api.md` accordingly (my earlier "source_format IN" claim was wrong) + documented the **half-open bucket-window** semantics (`from <= bucket_ts < to`, whole-bucket, no sub-bucket select — what keeps fast≡live). Stale-LSP false alarms dismissed by ground truth (`metricCost/metricCalls redeclared`, `smTokensIn undefined`, `floatEq redeclared` — all a mid-rename snapshot; `go build`/`vet`/golangci all clean, names are `sm*` vs the distinct `topologyMetric` consts). Tests (parity-first): **`TestStatsAggregate_FastPathMatchesLiveFold`** (84 subtests = 7 group_by × 6 metric × 2 bucket, fast≡live byte-for-byte) + `TestStatsTop_FastPathMatchesLiveFold` (30) + hand-computed closed+open, open-hour inclusion, fast-path-reads-rollups (wipe-table proof), mid-bucket-window parity, source_format/total shapes, filtered live-fold, n-clamp, dimension/enum/405/HEAD/empty. Gates (orchestrator-verified): gofmt 0, build 0, vet 0, **golangci 0**, gosec 0, presenter `-race` PASS (8.6s).
  - **Next:** Chunk 8b — `/api/search` (FTS5 `MATCH` bound `?q`, `bm25` rank, `snippet()`; join fts matches back to ops/sessions for `parseSessionFilter`; `logs_indexed` flag; offset cursor mirroring `/api/sessions/:id/logs`).

- **Chunk 8b — `/api/search` FTS endpoint (test-first; subagent-built, orchestrator-verified). Chunk 8 COMPLETE.** NEW `internal/presenter/search.go` `handleSearch` + `search_test.go`/`search_pagination_test.go` (split to keep files ≤400); +1 route. `fts_ops`/`fts_logs` queried by `MATCH ?` (bound — FTS5 syntax intentionally exposed, no injection), `ORDER BY bm25(...)` best-first, `snippet()` excerpts, JOINed back to `ops`⋈`sessions` (and `log_entries`⋈`sessions`⋈`sources` for logs) to apply `parseSessionFilter` constraints + render `kind`/`name`/`model`. **Correctness fix:** search OWNS `q`/`limit`/`cursor` — they're stripped before `parseSessionFilter` (else `q` would be applied as the session-list `agent_name LIKE` filter, silently excluding every row; `limit` would 400 at >1000). **`logs_indexed`** = `EXISTS(sources WHERE fts5_index_logs=1 [AND id IN <sources filter>])` (in-scope = the `sources` set if present, else all); when false the `fts_logs` query is skipped + `logs:[]`. **Cursor reconciliation (decided):** FTS ranked rows have no stable keyset key, so the cursor is an OFFSET into the fingerprint-pinned ranked set — reuses the `pageCursor` machinery + strict `decodeCursor` + the query-fingerprint stability guard (replay against a changed `q`/filter → 400); both arrays advance by the same offset; `next_cursor` minted iff either array filled the page. Tests (16, `-race`): basic op+log match (snippet/rank/linkage), bm25 ordering, each filter narrows, `logs_indexed` flip + sources-scoping, pagination + asymmetric-run-dry + fingerprint-mismatch/tamper/oversize → 400, validation (empty/whitespace/control-char `q` → 400; limit clamp; 405; HEAD), FTS5 phrase/prefix syntax honored, no-match empty arrays. Gates (orchestrator-verified): gofmt 0, build 0, vet 0, **golangci 0**, gosec 0, presenter `-race` PASS (10.6s). Stale-LSP `TestSearch_* redeclared` + `zz_ftsprobe_test.go` diagnostics dismissed by ground truth (tests live only in `search_pagination_test.go`; probe deleted; `search_test.go`=374L). **Follow-up filed (tracked, not deferred):** `SOW-0035` — a malformed FTS5 `q` currently returns 503 `DB_UNAVAILABLE` (surfaced+logged, but a client error misclassified); should map to 400. **The entire Go backend + REST/search API is now complete: ingest → store → rollups → FTS → `/api/stats/aggregate|top` + `/api/search`.**
  - **Next:** Chunk 9 — `/stats` dashboard page (React/TS + D3 line+bar in `viz/`, TanStack Query, SSE `stats_invalidated` refresh, the FilterBar wired to the new endpoints). Frontend domain — different toolchain (vitest/playwright/tsc/eslint).

## Validation

(Filled at end. Bench numbers, rollup-diff test result, review summary.)

## Reviews

(Filled as external reviewers run. One sub-section per round.)

## Outcome

Pending.

## Lessons Extracted

Pending.

## Followup

None yet.

## Regression Log

None yet.

Append regression entries here only after this SOW was completed or closed and later testing or use found broken behavior. Use a dated `## Regression - YYYY-MM-DD` heading at the end of the file. Never prepend regression content above the original SOW narrative.
